# Environment variables for the Scientific Calculator

# Port for development server (default: 3000)
PORT=3000

# Browser to open when starting dev server (default: default browser)
BROWSER=none

# Disable automatic browser opening
BROWSER=none
